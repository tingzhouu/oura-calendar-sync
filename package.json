{
  "name": "oura-calendar-sync-api",
  "version": "1.0.0",
  "description": "Backend API for Oura Calendar Sync",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed for Vercel functions'",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
